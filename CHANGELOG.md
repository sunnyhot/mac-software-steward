# Changelog

## v0.18.16 (2026-08-24)

### 修复：mas「No downloads initiated」被误分类为「尝试重新升级」

- `mas upgrade` 报 `Error: No downloads initiated for ADAM ID …` 时（购买请求成功但商店返回零下载），此前落入通用错误兜底，建议「尝试重新升级」，用户立即重试必然再次失败。根因是 App Store 发布传播延迟：商店目录已列出新版本（mas outdated 默认按目录比对），但购买/重下后端尚未向本机 Apple ID 放行该版本，常见于发布数小时内的新版本。
- 新增独立分类：失败原因明确为「App Store 已发布新版本，但尚未向本机账户开放下载」，建议改为稍后重试并在 App Store「更新」页确认，不再引导立即重试。

## v0.18.15 (2026-08-17)

### 修复：warp 等 auto_updates cask 已自更新却仍反复提示升级并下载失败

- warp 这类 auto_updates cask 会通过应用内更新器自行升级，Homebrew receipt 长期滞后。此前只有 manualUpdateOnly 的 cask 会按实际 .app 版本对齐，auto_updates 且 outdated 的 cask 仍被反复标记可升级；叠加 warp.dev 的 /download/brew?version= 下载端点已失效（404），每日巡检持续尝试下载并以「网络连接出现问题」失败。
- 版本对齐扩展到 auto_updates + outdated 的 cask：应用内版本已达到目标版本时（含 stable_00 与 00 这类渠道后缀差异，新增版本串归一化），停止标记 outdated/upgradeable，不再重复提示或尝试下载；应用版本确实低于目标版本时保持原行为。
- CaskMirrorPrefetcher 新增 warp.dev 直链改写：https://app.warp.dev/download/brew?version=vX → https://releases.warp.dev/stable/vX/Warp.dmg，配合既有 sha256 校验（不符不进入 brew 缓存），确有更新时 brew 走可用直链；缓存文件名仍按原始 URL 计算，brew 直接命中缓存。
- 每日巡检 agent 升级 cask 前同样执行镜像/直链预下载（此前仅 GUI 路径有），失败静默回退默认下载。

## v0.18.14 (2026-08-17)

### 改进：所有软件默认「自动升级」

- 默认升级策略统一为「自动升级」：formula、cask（含 greedy/auto_updates）、MAS 应用不再默认要求确认；风险提示（greedy cask、应用可能正在运行、major 版本变化等）改为仅展示信息，不再阻止自动升级。
- 每日巡检因此会直接自动升级所有未被硬阻断的更新：只有「策略设为跳过/仅提醒/确认后升级」「已固定」「源不可用」「不可执行」等才会被排除。
- 升级计划页中默认策略为自动的行始终预选；收件箱不再为会被自动升级的项目生成「需要确认升级」待办；巡检自动升级成功后，会移除对应包残留的「需要确认升级」待办。
- 设置页「低风险项目」开关文案改为「自动升级」，作为每日巡检自动升级的总门控。
- 如需对个别软件区别对待，可在升级策略里单独设置「确认后升级 / 仅提醒 / 跳过」，覆盖默认值。

### 修复：每日巡检「未发现升级」与一键升级口径不一致

- 巡检后台只把「自动升级」条件内的项当作「检测到升级」，遇到仅需确认的升级（如策略为「确认后升级」的 cask、有风险提示的 greedy cask）会打印「未发现可自动升级的软件」，而 GUI 的「一键升级」按钮统计所有可执行升级项，两者口径不一致，造成「巡检说没有、一键升级却有」的矛盾观感。
- 巡检结束现在会区分两种情况：确实没有任何升级时打印「未发现需要升级的软件」；检测到升级但不满足自动条件时打印「检测到 N 项可升级，但均需确认或不符合自动升级条件」。
- 巡检通知决策补传 availableUpgradeCount（与一键升级同口径的可执行升级集合，排除本轮已自动处理的项），通知从「有 N 项需要处理」变为「发现 N 个可升级项目」；同时剔除与升级计数重复的「需要确认升级」收件箱条目，避免同一批升级被重复计数。

### 修复：每日巡检系统通知永远无法送达

- 巡检 agent 由 launchd 以 helper 二进制启动，没有 LaunchServices 应用身份，UNUserNotificationCenter.requestAuthorization 直接抛 UNErrorDomain 错误 1（Notifications are not allowed for this application），巡检的通知（含「发现可升级项目」和失败提醒）从未真正送达，日志里每天只有一条投递失败记录。
- 改为：agent 巡检结束时把已决定的系统通知持久化为待发文件，GUI 应用下次启动时代为投递并清除。GUI 是正常的应用进程，通知授权与投递可以正常工作。

## v0.18.13 (2026-08-13)

### 修复：greedy 扫描把"含可升级项的批次"误判为失败（系统性漏报）

- `brew outdated` 在发现可升级项时以 **exit code 1** 退出（Homebrew 既定约定，非错误），JSON 仍完整输出到 stdout。v0.18.12 的分批 greedy 扫描用 `code == 0`（`CommandResult.ok`）判定每批成功与否，导致**凡含有可升级 cask 的批次都被整批判失败**，该批全部 cask（含真正可升级的）被丢进"未完成检查"名单——恰好在能发现更新时把更新丢弃，唯一"成功"的是没有可升级项的批次。
- 改为按「进程未被信号杀死 且 stdout 可解析为 brew outdated JSON」判定成功（与单命令非 greedy 路径一致）。新增纯函数 `BrewBatchedGreedy.greedyBatchSucceeded`（exit-1+合法 JSON / 超时 / 空 stdout / 非 JSON 四类用例覆盖）。
- 表现：之前 1password、qqmusic 等出现在"未完成检查的 cask"里实为可升级项被误藏；修复后它们会正常出现在可升级列表，"部分检查未完成"卡片仅在真正超时/失败时出现。

## v0.18.12 (2026-08-13)

### 改进：Homebrew greedy 扫描改为分批增量，慢网络不再整盘失败

- `brew outdated --greedy` 原先一次性检查所有 cask（全有或全无）。现按 cask 分批（每批 5、单批上限 60s、总预算 180s）串行执行：某批超时只丢该批，已扫到的结果照常返回，未扫到的 cask 在界面给出“部分检查未完成，建议稍后重试”的软提示（不建议 brew doctor）。Microsoft Office 这类最慢的 auto_updates cask 不再拖垮整次扫描。
- formulae 与 casks 的 outdated 拆分获取（formulae 用 `--formula`，本地比对、快）；casks 走分批 greedy。

### 修复：Homebrew 超时不再显示 Ruby 原始栈

- macOS Foundation 对被信号杀死的进程返回原始信号号（如 SIGTERM=15），不是 shell 的 128+信号号；旧 `code > 128` 判定永不成立，导致超时被 SIGTERM 时把 Homebrew 的 Ruby 栈原样抛给用户。改为依据 `terminationReason` 判定信号死亡，并对 Homebrew 自行捕获信号（以普通失败码退出、stderr 留栈）的情况加 stderr 内容兜底，统一翻译为“进程被终止 (SIGTERM)，可能是命令耗时过长被中断，请稍后重试。”。附带修正 mas 崩溃（SIGSEGV/SIGABRT）检测同理失效的问题。

## v0.18.11 (2026-08-06)

### 修复：定时巡检漏报 Homebrew 更新

- 在 `brew outdated` 扫描前先执行 `brew update` 刷新 Homebrew 元数据缓存，修复 `auto_updates` cask（如 Microsoft Office）因本地 API 缓存陈旧而漏报的问题；定时巡检此前会因此扫到 0 项并提前退出，导致升级阶段的 `brew update` 永不触发，缓存长期不刷新。
- 刷新失败不阻断扫描，沿用既有容错策略（用旧缓存继续，宁可漏检也不让整个 Homebrew 扫描失败）。此改动在共享的 `Scanner.scanBrew` 中生效，GUI 手动检测与定时巡检同时受益。

## v0.18.10 (2026-08-04)

### 修复：自更新无限下载循环

- 修复 v0.18.9 因发布清单（latest.json）校验和与实际安装包不符，导致每次自检都下载、SHA 校验失败后反复重下的死循环。
- 自更新新增「失败版本记忆」：某版本安装包校验或安装失败后，记住其版本号与校验和，后续自动检查命中同一安装包时跳过自动下载，避免坏发布触发无限重下；手动检查与更新不受影响，出现新版本或同版本重新打包后记忆自动失效。

### 修复：Homebrew 扫描误报

- 将 `brew outdated`（含 greedy 联网检查）的扫描超时由 30 秒延长至 120 秒，避免命令耗时较长时被中途终止。
- Homebrew 扫描命令被超时终止时，错误提示改为「更新检查超时，请稍后重试」，不再甩出 Ruby SIGTERM 栈或误导用户去运行 `brew doctor`。

## v0.18.9 (2026-08-04)

### 界面响应与动画

- 搜索输入拆分为独立的轻量状态，快速输入不再让整棵维护界面逐键重绘。
- 列表筛选、排序等派生数据仅在值变化时刷新，并在升级进度更新时避免重复全量转换。
- 行悬停缩放、状态脉冲、列表过渡等动画在「减少动态效果」时关闭，页面切换更跟手。
- 软件清单与升级页的展示行缓存到本地状态，搜索和筛选只重算列表、不触发布局抖动。

### 键盘导航与搜索

- 新增「导航」菜单：⌘1 切换可升级、⌘2 切换本机软件、⌘F 聚焦搜索框。
- ⌘, 直接打开设置；搜索框支持回车立即应用、清除按钮一键清空。

### 扫描与缓存

- 应用更新源解析改为批量写入缓存，一轮扫描只落盘一次，减少磁盘抖动。
- 扫描阶段文案调整为本机应用、Homebrew、App Store 并行扫描，并在整理结果时给出收尾提示。

### 设置与外观

- 升级页与软件清单头部改为带分隔的卡片式布局，筛选器宽度自适应。
- 高级升级选项改为可记忆展开状态，并新增网络策略、自动修复策略等设置项入口。

## v0.18.8 (2026-08-04)

### 每日巡检可靠性

- 自动识别并修复指向旧版应用、文件已丢失或尚未加载的每日巡检 Helper，同时保留原计划时间和高级选项。
- 后台升级改为逐项执行；单个软件或 `brew update` 失败不会阻断其余项目，并在巡检报告中汇总全部失败。
- 设置页显示调度健康状态、最近退出码和一键修复入口；失败时可直接查看日志，并按通知策略发送系统通知。
- 持久保存 greedy Cask 与升级前 `brew update` 选项，开发构建不会把正式任务误绑到临时目录。

### 扫描与残留清理体验

- 移除 Homebrew 残留记录成功后直接局部更新列表，不再触发全量扫描或丢失当前滚动位置。
- 首次扫描使用完整占位页，后续刷新保留已有列表并显示轻量进度；刷新期间不再展示上一轮来源错误。
- Homebrew / App Store 来源错误卡片改为紧凑摘要，原始错误和诊断命令收进可展开详情，并显示最近检查时间。

## v0.18.7 (2026-08-03)

### 升级渠道修复

- 当厂商已发布新版本、但当前 Homebrew Cask 尚未收录时，升级页会解释渠道差异，不再重复执行无效的 `brew upgrade`。
- 对 Homebrew 已提供对应预览渠道的项目，可在明确确认后直接切换渠道；例如将 `hyper` 切换为 `hyper@canary`。
- 应用通过内置更新器或官方安装包升级后，以实际 `.app` 版本复核结果，达到目标版本即停止重复提醒。

### Homebrew 残留记录清理

- 根据 Cask 声明的 `.app` artifact 路径识别“应用已删除、Homebrew receipt 仍存在”的残留记录，字体、插件和驱动类 Cask 不会被误判。
- 升级页会显示缺失路径并提供“移除残留记录”，确认后执行 `brew uninstall --cask --force`，但不使用 `--zap` 删除个人配置。
- 清理前再次检查应用路径；如果 App 已重新出现，则停止执行，避免使用过期扫描结果误删。

## v0.18.6 (2026-08-03)

### 升级提醒

- 扫描发现可执行升级后，在主窗口显示升级提醒，提供“查看升级”和“一键升级”操作。
- “只在需要处理时通知”现在会对新发现的 Homebrew / App Store 升级发送系统通知，不再只提醒来源问题或普通 App 更新。
- 应用在前台时也会展示通知横幅，点击通知可唤起主窗口。
- 同一批升级在单次运行中只通知一次，重复扫描不会反复提醒；新出现的升级仍会正常通知。

## v0.18.5 (2026-08-03)

### 可升级列表修复

- 侧栏、可升级列表、菜单栏与“一键升级”统一使用同一套可执行升级数据，不再出现侧栏有数量但列表为空的情况。
- `auto_updates` Cask 即使未开启全局 greedy 扫描，只要已发现新版本，也会显示在可升级列表并通过逐包 `--greedy` 纳入一键升级。
- 空状态会区分“确实没有升级”和“当前筛选无结果”，避免误导。

## v0.18.4 (2026-08-03)

### Homebrew Cask 下载优化

- Cask 升级前自动尝试通过国内 GitHub 镜像预下载 Release 资产到 Homebrew 缓存，改善 GitHub 直连较慢时的升级体验。
- 预下载仅作用于 GitHub Releases，并校验 Cask 声明的 SHA-256；镜像不可用或校验失败时自动回退到 Homebrew 原有下载流程。

## v0.18.3 (2026-08-03)

### 菜单栏优化

- 精简菜单栏下拉菜单，移除与菜单栏图标标题重复的状态文字和运行中文案，只保留操作项。

## v0.18.2 (2026-08-03)

### 性能优化

- 节流升级进度更新频率到约 10Hz，下载/升级时不再逐行 stdout 触发全量 UI 重算，滚动更流畅。
- 可升级列表的筛选+排序在每次界面刷新时只计算一次，不再重复两次全量计算。
- 移除下载进度详情中的流动动画，减少并发下载时的 GPU 合成负担。
- 移除可升级列表每行的 hover 缩放动画和列表项插入/移除过渡动画。
- 收件箱条目扫描后批量写入，不再逐条全量编码写盘。

## v0.18.1 (2026-08-03)

### 可升级体验优化

- auto_updates 类 Cask（如 1Password、Microsoft 系列）现在可通过 `brew upgrade --cask --greedy` 正常升级，不再误标为「需手动」。
- 可升级列表不再显示无法升级的包，「需手动」状态字样已移除。
- 可升级列表排序调整：正在下载或升级中的包优先显示在最前面。
- 可升级列表每行的升级/重试按钮改为带边框样式，点击区域更大、更醒目。

## v0.18.0 (2026-08-03)

### 界面精简

- 合并「自动化策略」页进「设置」页，所有配置集中一处；侧边栏精简为可升级、本机软件、设置三个标签。
- 移除「维护总览」和「任务日志」页。
- 可升级页每行精简：移除逐包策略下拉框、来源文字胶囊和固定/自更新徽章。
- 设置页精简：隐藏失败恢复和普通 App 联网检查，通知选项从 4 个简化为 2 个。

### 新功能

- 可升级页新增「一键升级」按钮，批量升级所有可执行项；批量任务作为单个 job 执行，需要管理员密码的 Cask 只弹一次系统密码框。

### 问题修复

- 修复扫描恢复正常后「Homebrew 扫描错误」等收件箱条目永久残留的问题。
- 修复浅色系统下「跟随系统 → 深色 → 跟随系统」后窗口出现深浅混搭的问题。
- 移除普通 App 联网检查中的「积极检查公开页面」选项（该选项无实际效果）。

## v0.17.0 (2026-07-29)

### 功能收敛

- 统一“开始智能维护”和“一键升级”为“检查并维护”：每次重新扫描后生成维护计划，用户确认后再执行。
- 普通 App 只保留应用内与厂商更新器指引，移除下载安装包并直接覆盖 App 的高风险路径。
- 本机软件页回归资产与诊断视图，Homebrew 和 App Store 的升级操作统一放在可升级页。
- 自动化策略移除“自动化管家”总开关与首次引导，只保留每日巡检和低风险项目处理方式；低频升级参数收进高级选项。
- 删除不再使用的直接替换实现、自动化访问门控和共享 UI 残留。

## v0.15.1 (2026-07-27)

### 自更新弹框

- 收敛自更新弹框字号到 macOS 标准节奏，与软件其余界面协调：主标题由 27pt 降到 20pt，更新说明正文由 15pt 降到 12pt，眉题/副标题/下载状态同步下调，应用图标从 56pt 收到 48pt。

## v0.15.0 (2026-07-27)

### 功能与架构收敛

- 移除不可访问的待处理、管理来源、历史和性能独立页面；待处理事项合并到维护总览，来源诊断合并到可升级页，巡检/升级/待办历史合并到任务日志。
- 移除高级模式，核心维护、自动化策略和任务日志入口始终可见；普通 App 更新诊断直接通过折叠详情提供。
- 移除未接入生产流程的维护工作流状态机、运行记录、租约、校验/恢复协调器，以及已经没有界面入口的自动化数据导入导出和旧规则控制台。
- 停止持久化不可见的扫描性能历史，保留单次扫描阶段耗时用于扫描诊断。
- 恢复原生 macOS 标题栏、侧栏切换和统一工具栏，删除自定义窗口 chrome 与双击缩放补丁。

## v0.14.0 (2026-07-10)

### 统一维护引擎与维护总览页

这是自 v0.13.31 以来的架构性升级，引入统一维护引擎和新的默认「维护总览」页面。

#### 维护总览页（新默认入口）

- **应用启动后默认进入「维护总览」页**，标准模式和高级模式都是。
- **「开始智能维护」按钮**：扫描本机软件 → 分类（automatic / 需确认 / 仅提醒 / 已阻止）→ 只自动升级低风险项 → 校验结果。
- **维护轨道可视化**：扫描 → 评估 → 执行 → 校验，四阶段连续展示，当前阶段高亮，已完成标绿。
- **四项指标卡片**：管理软件总数、可自动升级、需确认、下次巡检时间。
- **优先任务列表**：展示可自动升级和需确认的项目。
- **维护摘要**：最近一次运行结果 + 每日巡检状态。
- 首次启动（未扫描）显示就绪态，不显示零值健康评估。

#### 统一计划分类

- 新增 `MaintenancePlanner`，把扫描结果分类为 automatic / confirmationRequired / reminderOnly / blocked 四种 disposition。GUI 和后台 Agent 共用同一套分类逻辑。
- **`lowRiskAutoUpgradeEnabled` 不再形同虚设**：此前该开关在计划/巡检链路中完全没被读取；现在它真正门控 automatic 组——关闭时所有项降级为需确认。**这是行为变化**。
- blocked 项（跳过 / 源不可用 / 固定 / 不可升级 / 风险阻止）不携带可执行包，UI 无法转为可执行。

#### 执行引擎重构

- 新增 `MaintenanceExecutor`（1138 行），从 `StewardModel` 抽出全部升级执行逻辑（任务队列、并发控制、取消令牌、包级进度、下载加速、Homebrew 下载监控、失败分析、自动修复）。
- `StewardModel` 从 1628 行降到 696 行（-57%），降级为门面：通过 computed property 转发 jobs / packageProgress / upgradeProgress，通过 objectWillChange 合并让现有 UI 无感切换。
- 修复已有 bug：`strategiesForStep` 改用注入的 `downloadStrategiesProvider`，不再硬调 `DownloadAccelerationPolicy.defaultStrategies()`。

#### 跨进程租约与运行记录

- 新增 `MaintenanceRunLease`：Foundation-only 跨进程锁（flock 独占 + PID/启动时间防复用 + stale 回收），GUI 与后台 Agent 共用，防止同时执行。
- 新增 `MaintenanceRunStore`：版本化运行记录（触发来源、时间、终态、计划分类、逐包结果），存储到 `~/Library/Application Support/MacSoftwareSteward/maintenance-runs.json`。
- 自动化数据导入导出升级到 schema v3，新增 `maintenanceRunRecords` 字段，向后兼容 v1/v2。

#### 其他

- 新增 `MaintenanceVerifier`（重扫后校验：命令成功但版本仍 outdated → mismatch）和 `MaintenanceRecoveryCoordinator`（复用 RecoveryActionPlanner）。
- 新增 `MaintenanceWorkflowState` 工作流状态机（idle → scanning → assessing → executing → verifying → 终态）。
- 后台 Agent 改用统一 `MaintenancePlanner` 做计划分类。
- 76 个测试全绿（含 8 个新增维护引擎测试）。

## v0.13.31 (2026-07-09)

### 失败孤儿可见性修复

- **修复状态横幅与升级列表对不上的问题**：升级失败后重新扫描，如果包不再 outdated，它会从可升级列表消失，但失败记录仍残留在 `packageProgress` 中，导致顶部状态横幅显示“有 N 个升级需要处理”、而下方“可执行升级”列表却是空的，用户既看不到也操作不了。
- **失败孤儿在升级列表单独成区展示**：新增 `StewardModel.orphanedFailedProgresses`，把不在当前可升级集合中的 failed/timedOut/cancelled 记录暴露出来；升级页在列表下方渲染孤儿区，标注“N 个失败项不在当前可升级列表中”，复用红色失败视觉、失败原因详情与恢复建议。
- **孤儿可重试、可清除**：每条孤儿行提供“重试”（走已有的 `retryPackage`，包不在列表时会触发重新扫描）和“清除”按钮；区头提供“全部清除”批量操作。
- **回归测试**：新增 4 组断言，覆盖孤儿暴露、失败包重新出现后不再算孤儿、`clearPackageFailure` 移除孤儿，以及 succeeded/queued/running/warning 状态不计入孤儿。

## v0.13.28 (2026-07-03)

### 窗口头部修复

- **移除多余系统工具栏控件**：主窗口保留原生红黄绿按钮，但移除 `NavigationSplitView` 自动生成的侧栏切换按钮，避免头部区域出现额外控件。
- **统一头部层级**：窗口标题栏透明化并延展内容层，让交通灯区域、侧栏和主内容 header 更像一套连续的 macOS 顶部结构。
- **收紧顶部留白**：同步侧栏和详情 header 的顶部节奏，减少扫描卡片上方的空白感。
- **回归测试**：新增窗口 chrome 策略测试，防止标题栏、sidebar toggle 和左右头部 spacing 再次漂移。

## v0.13.27 (2026-07-03)

### 窗口布局收口

- **统一顶部 chrome**：主窗口显式使用系统窗口工具栏背景，并在内容层增加不可交互的顶部 rail，让交通灯区域、侧栏和主内容 header 形成连续的 macOS 顶部结构。
- **侧栏节奏微调**：侧栏顶部留白与主内容 header 对齐，减少左上角灰块和内容区之间的割裂感。
- **发布验证**：保留 `NavigationSplitView` 和现有任务优先导航，未改动扫描、升级、自动化或持久化逻辑。

## v0.13.24 (2026-06-29)

### UI 与状态动效升级

- **维护状态带**：主界面新增维护状态带，统一展示扫描、升级、失败处理和待升级数量。
- **侧栏状态升级**：侧栏状态 chip 改为复用维护状态 presenter，扫描、升级、待处理和已最新状态文案保持一致。
- **共享视觉组件**：新增语义状态色、图标板、流动强调线和任务表面样式，统一空状态、警告、安装提示和升级进度条质感。
- **可升级页面打磨**：更新筛选区、扫描中状态、升级任务卡片和包级进度细节，运行中任务增加流动强调反馈。
- **回归测试补充**：新增维护状态 presenter 测试，覆盖空闲、待升级、扫描、升级中和失败待处理状态。

## v0.13.23 (2026-06-22)

### 导航与本机软件体验

- **侧栏信息架构收敛**：移除待处理、管理来源、历史、性能等低频入口，将可升级、本机软件、自动化策略和任务日志按日常维护/诊断控制重新分组。
- **本机软件统计修正**：本机软件总数与列表保持一致，区分普通 App、Homebrew 和 App Store 来源，系统内不可人工维护的软件不再混入主列表。
- **普通 App 更新指引升级**：Sparkle 发现新版本时展示更明确的“打开应用更新”与“直接替换（风险自负）”选项；直接替换支持 `.zip/.dmg` 包并校验 bundle id。
- **更新诊断文案调整**：“更新源需确认”改为“无法确认更新源”，降低状态理解成本。
- **自动化策略收口**：设置页移除重复的自动化管家入口，自动化策略、风险规则和恢复规则集中在自动化策略页。

## v0.13.22 (2026-06-22)

### 自更新热修

- **修复下载完成后未自动安装**：自更新安装脚本改用 macOS 实际存在的 `/bin/test` 校验新 app bundle，避免脚本在替换前因 `/usr/bin/test` 不存在而失败。
- **防止重复下载循环**：补充回归测试，确保安装脚本不会再引用不存在的 `/usr/bin/test`，下载、解压后能继续进入替换和重启流程。

## v0.13.21 (2026-06-16)

### 扫描性能优化

- **Sparkle appcast 有界并发**：普通 App 的 Sparkle feed 检查从串行改为默认 4 路有界并发，并保留原始应用列表顺序。
- **Sparkle feed 超时保护**：每个 appcast 请求使用 8 秒显式 timeout，超时时给出独立诊断，避免单个慢 feed 拖垮整次扫描。
- **普通 App 更新能力缓存**：基于 `Info.plist` 修改时间和文件大小缓存本机更新能力识别结果，减少重复扫描时的 plist 读取和解析。
- **性能可观测性收口**：扫描阶段耗时继续覆盖普通 App discovery 和 Sparkle appcast 阶段，便于在高级模式性能页对比优化效果。

## v0.13.20 (2026-06-15)

### 高级诊断与发布收口

- **普通 App 诊断细节增强**：高级模式下展示更新源异常、无法确认版本、识别器、置信度、安装/可用版本、Feed URL 和建议处理动作。
- **厂商更新器规则补充**：普通 App 更新能力识别新增 Chrome Keystone、Adobe、JetBrains Toolbox、Microsoft AutoUpdate 的 Info.plist 元数据证据。
- **规则/历史视图打磨**：规则页支持分类和搜索筛选，规则行可展开查看细节；历史页统一巡检、升级、待办处理记录，支持分类、状态和搜索筛选。
- **导入导出完善**：自动化数据 bundle 升级到 schema v2，纳入升级/待办历史；导入前展示策略、报告、历史数量，并兼容 schema v1 文件。
- **发布前收口**：版本号同步到 0.13.20，README 补充高级模式、导入导出和 package 流程说明。

## v0.12.0 (2026-05-28)

### 改进管理来源与下载更新的错误提示和恢复机制

- **管理来源报错展示可执行修复建议**：新增 SourceDiagnosticEngine 诊断引擎，自动分析 Homebrew/mas 错误并生成中文原因 + 建议操作；新增 ErrorRecoveryCard 通用恢复组件，支持重新扫描、安装工具、查看手动命令等一键操作
- **自更新下载失败保留失败态**：下载/解压/安装失败时保留失败状态，不再静默回退到发现新版本；新增 friendlyUpdateErrorMessage 友好错误翻译，网络/取消/超时等场景显示中文提示；弹窗与设置页同步展示失败信息和重试入口
- **集成验证通过**：管理来源错误提示与自更新失败恢复机制代码集成无冲突，构建验证通过

### 涉及子 issue

- [LUC-241] 管理来源报错展示可执行修复建议与自动处理入口
- [LUC-242] 自更新下载失败保留失败态并及时提示错误
- [LUC-243] 统一验证管理来源与自更新错误提示流程

# Changelog

## v0.9.2 (2026-05-22)

### UI 优化

- **整体布局专业化重构**：主窗口视觉基线统一，全应用采用 .regularMaterial 背景、.rounded 字体、12pt/10pt 圆角规范
- **组件化提取**：HeaderButton、MetricCard、SettingsDivider、JobNoticeIcon 等可复用组件
- **macOS 版本适配**：symbolEffect(.pulse/.rotate) 等 macOS 15+ API 添加 #available 守卫，兼容 macOS 14
- **动效细节打磨**：hover 缩放、spring 动画参数优化，克制且有质感
- **四个页面全部覆盖**：UpdatesView（可升级列表）、ApplicationsView（本机应用）、SourcesView（管理来源）、JobsView（任务日志）+ SettingsView（设置页）+ ContentView（主框架）

### 涉及子 issue

- [LUC-210] 主窗口布局与视觉基线
- [LUC-211] 可升级与本机应用列表信息架构
- [LUC-212] 管理来源与设置页面板体验
- [LUC-213] 任务日志与动效细节

## v0.9.1 (2026-05-21)

### 新功能

- **开机自启动时自动隐藏 Dock 图标**：应用通过开机自启动方式启动时，自动读取设置中的 Dock 图标显示偏好。若用户已设置不显示 Dock 图标，则在自启动完成后自动隐藏，无需用户手动触发。（[LUC-190]）

### 基础设施

- **GitHub Actions Release 工作流**：添加 CI/CD 自动发布工作流

### 涉及子 issue

- [LUC-190] 实现 AppDelegate 提前设置 Dock 策略 + 启动时隐藏窗口

## v0.9.0 (2026-05-20)

### 性能优化

- **ContentView 拆分**：将 1618 行巨型视图拆分为独立视图文件（UpdatesView/AppsView/JobsView/LogView/SettingsView），改善 SwiftUI 渲染性能
- **扫描进度指示**：扫描期间显示当前阶段（本机应用/Homebrew/App Store），改善等待体验
- **搜索防抖**：200ms 防抖避免每次按键全量过滤，cachedUpgradeablePackages 和 availableUpdates 从 computed property 改为 @Published 缓存
- **Brew 扫描超时隔离**：各 brew 子命令独立超时（30s），单个失败不阻塞其他结果
- **JobsView 日志优化**：LazyVStack 逐行渲染，1500 条日志不卡顿
- **classify 算法优化**：O(apps × casks) → O(apps + casks)，normalizeToken 正则结果缓存

### 涉及子 issue
- [LUC-146] 扫描进度指示
- [LUC-147] ContentView 拆分
- [LUC-148] 搜索防抖 + 缓存 computed property
- [LUC-149] Brew 扫描超时独立处理
- [LUC-150] JobsView 日志 LazyVStack 优化
- [LUC-151] classify 算法优化 + normalizeToken 缓存

## [0.12.1] - 2026-05-29

### Fixed
- 添加完整 codesign 签名步骤到 build-native.sh，修复 macOS 应用「租车总成本比较」打开报错"可能已损坏或不完整"的问题 (LUC-251)
  - Sparkle.framework ad-hoc 签名
  - codesign --deep --force --sign - 整包签名
  - codesign --verify --deep --strict 验证
  - xattr -cr 清除隔离属性
