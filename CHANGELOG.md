# Changelog

## v0.18.1 (2026-08-03)

### 可升级体验优化

- auto_updates 类 Cask（如 1Password、Microsoft 系列）现在可通过 `brew upgrade --cask --greedy` 正常升级，不再误标为「需手动」。
- 可升级列表不再显示无法升级的包，「需手动」状态字样已移除。
- 可升级列表排序调整：正在下载或升级中的包优先显示在最前面。
- 可升级列表每行的升级/重试按钮改为带边框样式，点击区域更大、更醒目。

## v0.18.0 (2026-08-03)

### 界面精简

- 合并「自动化策略」页进「设置」页，所有配置集中一处；侧边栏精简为可升级、本机软件、设置三个标签。
- 移除「维护总览」和「任务日志」页。
- 可升级页每行精简：移除逐包策略下拉框、来源文字胶囊和固定/自更新徽章。
- 设置页精简：隐藏失败恢复和普通 App 联网检查，通知选项从 4 个简化为 2 个。

### 新功能

- 可升级页新增「一键升级」按钮，批量升级所有可执行项；批量任务作为单个 job 执行，需要管理员密码的 Cask 只弹一次系统密码框。

### 问题修复

- 修复扫描恢复正常后「Homebrew 扫描错误」等收件箱条目永久残留的问题。
- 修复浅色系统下「跟随系统 → 深色 → 跟随系统」后窗口出现深浅混搭的问题。
- 移除普通 App 联网检查中的「积极检查公开页面」选项（该选项无实际效果）。

## v0.17.0 (2026-07-29)

### 功能收敛

- 统一“开始智能维护”和“一键升级”为“检查并维护”：每次重新扫描后生成维护计划，用户确认后再执行。
- 普通 App 只保留应用内与厂商更新器指引，移除下载安装包并直接覆盖 App 的高风险路径。
- 本机软件页回归资产与诊断视图，Homebrew 和 App Store 的升级操作统一放在可升级页。
- 自动化策略移除“自动化管家”总开关与首次引导，只保留每日巡检和低风险项目处理方式；低频升级参数收进高级选项。
- 删除不再使用的直接替换实现、自动化访问门控和共享 UI 残留。

## v0.15.1 (2026-07-27)

### 自更新弹框

- 收敛自更新弹框字号到 macOS 标准节奏，与软件其余界面协调：主标题由 27pt 降到 20pt，更新说明正文由 15pt 降到 12pt，眉题/副标题/下载状态同步下调，应用图标从 56pt 收到 48pt。

## v0.15.0 (2026-07-27)

### 功能与架构收敛

- 移除不可访问的待处理、管理来源、历史和性能独立页面；待处理事项合并到维护总览，来源诊断合并到可升级页，巡检/升级/待办历史合并到任务日志。
- 移除高级模式，核心维护、自动化策略和任务日志入口始终可见；普通 App 更新诊断直接通过折叠详情提供。
- 移除未接入生产流程的维护工作流状态机、运行记录、租约、校验/恢复协调器，以及已经没有界面入口的自动化数据导入导出和旧规则控制台。
- 停止持久化不可见的扫描性能历史，保留单次扫描阶段耗时用于扫描诊断。
- 恢复原生 macOS 标题栏、侧栏切换和统一工具栏，删除自定义窗口 chrome 与双击缩放补丁。

## v0.14.0 (2026-07-10)

### 统一维护引擎与维护总览页

这是自 v0.13.31 以来的架构性升级，引入统一维护引擎和新的默认「维护总览」页面。

#### 维护总览页（新默认入口）

- **应用启动后默认进入「维护总览」页**，标准模式和高级模式都是。
- **「开始智能维护」按钮**：扫描本机软件 → 分类（automatic / 需确认 / 仅提醒 / 已阻止）→ 只自动升级低风险项 → 校验结果。
- **维护轨道可视化**：扫描 → 评估 → 执行 → 校验，四阶段连续展示，当前阶段高亮，已完成标绿。
- **四项指标卡片**：管理软件总数、可自动升级、需确认、下次巡检时间。
- **优先任务列表**：展示可自动升级和需确认的项目。
- **维护摘要**：最近一次运行结果 + 每日巡检状态。
- 首次启动（未扫描）显示就绪态，不显示零值健康评估。

#### 统一计划分类

- 新增 `MaintenancePlanner`，把扫描结果分类为 automatic / confirmationRequired / reminderOnly / blocked 四种 disposition。GUI 和后台 Agent 共用同一套分类逻辑。
- **`lowRiskAutoUpgradeEnabled` 不再形同虚设**：此前该开关在计划/巡检链路中完全没被读取；现在它真正门控 automatic 组——关闭时所有项降级为需确认。**这是行为变化**。
- blocked 项（跳过 / 源不可用 / 固定 / 不可升级 / 风险阻止）不携带可执行包，UI 无法转为可执行。

#### 执行引擎重构

- 新增 `MaintenanceExecutor`（1138 行），从 `StewardModel` 抽出全部升级执行逻辑（任务队列、并发控制、取消令牌、包级进度、下载加速、Homebrew 下载监控、失败分析、自动修复）。
- `StewardModel` 从 1628 行降到 696 行（-57%），降级为门面：通过 computed property 转发 jobs / packageProgress / upgradeProgress，通过 objectWillChange 合并让现有 UI 无感切换。
- 修复已有 bug：`strategiesForStep` 改用注入的 `downloadStrategiesProvider`，不再硬调 `DownloadAccelerationPolicy.defaultStrategies()`。

#### 跨进程租约与运行记录

- 新增 `MaintenanceRunLease`：Foundation-only 跨进程锁（flock 独占 + PID/启动时间防复用 + stale 回收），GUI 与后台 Agent 共用，防止同时执行。
- 新增 `MaintenanceRunStore`：版本化运行记录（触发来源、时间、终态、计划分类、逐包结果），存储到 `~/Library/Application Support/MacSoftwareSteward/maintenance-runs.json`。
- 自动化数据导入导出升级到 schema v3，新增 `maintenanceRunRecords` 字段，向后兼容 v1/v2。

#### 其他

- 新增 `MaintenanceVerifier`（重扫后校验：命令成功但版本仍 outdated → mismatch）和 `MaintenanceRecoveryCoordinator`（复用 RecoveryActionPlanner）。
- 新增 `MaintenanceWorkflowState` 工作流状态机（idle → scanning → assessing → executing → verifying → 终态）。
- 后台 Agent 改用统一 `MaintenancePlanner` 做计划分类。
- 76 个测试全绿（含 8 个新增维护引擎测试）。

## v0.13.31 (2026-07-09)

### 失败孤儿可见性修复

- **修复状态横幅与升级列表对不上的问题**：升级失败后重新扫描，如果包不再 outdated，它会从可升级列表消失，但失败记录仍残留在 `packageProgress` 中，导致顶部状态横幅显示“有 N 个升级需要处理”、而下方“可执行升级”列表却是空的，用户既看不到也操作不了。
- **失败孤儿在升级列表单独成区展示**：新增 `StewardModel.orphanedFailedProgresses`，把不在当前可升级集合中的 failed/timedOut/cancelled 记录暴露出来；升级页在列表下方渲染孤儿区，标注“N 个失败项不在当前可升级列表中”，复用红色失败视觉、失败原因详情与恢复建议。
- **孤儿可重试、可清除**：每条孤儿行提供“重试”（走已有的 `retryPackage`，包不在列表时会触发重新扫描）和“清除”按钮；区头提供“全部清除”批量操作。
- **回归测试**：新增 4 组断言，覆盖孤儿暴露、失败包重新出现后不再算孤儿、`clearPackageFailure` 移除孤儿，以及 succeeded/queued/running/warning 状态不计入孤儿。

## v0.13.28 (2026-07-03)

### 窗口头部修复

- **移除多余系统工具栏控件**：主窗口保留原生红黄绿按钮，但移除 `NavigationSplitView` 自动生成的侧栏切换按钮，避免头部区域出现额外控件。
- **统一头部层级**：窗口标题栏透明化并延展内容层，让交通灯区域、侧栏和主内容 header 更像一套连续的 macOS 顶部结构。
- **收紧顶部留白**：同步侧栏和详情 header 的顶部节奏，减少扫描卡片上方的空白感。
- **回归测试**：新增窗口 chrome 策略测试，防止标题栏、sidebar toggle 和左右头部 spacing 再次漂移。

## v0.13.27 (2026-07-03)

### 窗口布局收口

- **统一顶部 chrome**：主窗口显式使用系统窗口工具栏背景，并在内容层增加不可交互的顶部 rail，让交通灯区域、侧栏和主内容 header 形成连续的 macOS 顶部结构。
- **侧栏节奏微调**：侧栏顶部留白与主内容 header 对齐，减少左上角灰块和内容区之间的割裂感。
- **发布验证**：保留 `NavigationSplitView` 和现有任务优先导航，未改动扫描、升级、自动化或持久化逻辑。

## v0.13.24 (2026-06-29)

### UI 与状态动效升级

- **维护状态带**：主界面新增维护状态带，统一展示扫描、升级、失败处理和待升级数量。
- **侧栏状态升级**：侧栏状态 chip 改为复用维护状态 presenter，扫描、升级、待处理和已最新状态文案保持一致。
- **共享视觉组件**：新增语义状态色、图标板、流动强调线和任务表面样式，统一空状态、警告、安装提示和升级进度条质感。
- **可升级页面打磨**：更新筛选区、扫描中状态、升级任务卡片和包级进度细节，运行中任务增加流动强调反馈。
- **回归测试补充**：新增维护状态 presenter 测试，覆盖空闲、待升级、扫描、升级中和失败待处理状态。

## v0.13.23 (2026-06-22)

### 导航与本机软件体验

- **侧栏信息架构收敛**：移除待处理、管理来源、历史、性能等低频入口，将可升级、本机软件、自动化策略和任务日志按日常维护/诊断控制重新分组。
- **本机软件统计修正**：本机软件总数与列表保持一致，区分普通 App、Homebrew 和 App Store 来源，系统内不可人工维护的软件不再混入主列表。
- **普通 App 更新指引升级**：Sparkle 发现新版本时展示更明确的“打开应用更新”与“直接替换（风险自负）”选项；直接替换支持 `.zip/.dmg` 包并校验 bundle id。
- **更新诊断文案调整**：“更新源需确认”改为“无法确认更新源”，降低状态理解成本。
- **自动化策略收口**：设置页移除重复的自动化管家入口，自动化策略、风险规则和恢复规则集中在自动化策略页。

## v0.13.22 (2026-06-22)

### 自更新热修

- **修复下载完成后未自动安装**：自更新安装脚本改用 macOS 实际存在的 `/bin/test` 校验新 app bundle，避免脚本在替换前因 `/usr/bin/test` 不存在而失败。
- **防止重复下载循环**：补充回归测试，确保安装脚本不会再引用不存在的 `/usr/bin/test`，下载、解压后能继续进入替换和重启流程。

## v0.13.21 (2026-06-16)

### 扫描性能优化

- **Sparkle appcast 有界并发**：普通 App 的 Sparkle feed 检查从串行改为默认 4 路有界并发，并保留原始应用列表顺序。
- **Sparkle feed 超时保护**：每个 appcast 请求使用 8 秒显式 timeout，超时时给出独立诊断，避免单个慢 feed 拖垮整次扫描。
- **普通 App 更新能力缓存**：基于 `Info.plist` 修改时间和文件大小缓存本机更新能力识别结果，减少重复扫描时的 plist 读取和解析。
- **性能可观测性收口**：扫描阶段耗时继续覆盖普通 App discovery 和 Sparkle appcast 阶段，便于在高级模式性能页对比优化效果。

## v0.13.20 (2026-06-15)

### 高级诊断与发布收口

- **普通 App 诊断细节增强**：高级模式下展示更新源异常、无法确认版本、识别器、置信度、安装/可用版本、Feed URL 和建议处理动作。
- **厂商更新器规则补充**：普通 App 更新能力识别新增 Chrome Keystone、Adobe、JetBrains Toolbox、Microsoft AutoUpdate 的 Info.plist 元数据证据。
- **规则/历史视图打磨**：规则页支持分类和搜索筛选，规则行可展开查看细节；历史页统一巡检、升级、待办处理记录，支持分类、状态和搜索筛选。
- **导入导出完善**：自动化数据 bundle 升级到 schema v2，纳入升级/待办历史；导入前展示策略、报告、历史数量，并兼容 schema v1 文件。
- **发布前收口**：版本号同步到 0.13.20，README 补充高级模式、导入导出和 package 流程说明。

## v0.12.0 (2026-05-28)

### 改进管理来源与下载更新的错误提示和恢复机制

- **管理来源报错展示可执行修复建议**：新增 SourceDiagnosticEngine 诊断引擎，自动分析 Homebrew/mas 错误并生成中文原因 + 建议操作；新增 ErrorRecoveryCard 通用恢复组件，支持重新扫描、安装工具、查看手动命令等一键操作
- **自更新下载失败保留失败态**：下载/解压/安装失败时保留失败状态，不再静默回退到发现新版本；新增 friendlyUpdateErrorMessage 友好错误翻译，网络/取消/超时等场景显示中文提示；弹窗与设置页同步展示失败信息和重试入口
- **集成验证通过**：管理来源错误提示与自更新失败恢复机制代码集成无冲突，构建验证通过

### 涉及子 issue

- [LUC-241] 管理来源报错展示可执行修复建议与自动处理入口
- [LUC-242] 自更新下载失败保留失败态并及时提示错误
- [LUC-243] 统一验证管理来源与自更新错误提示流程

# Changelog

## v0.9.2 (2026-05-22)

### UI 优化

- **整体布局专业化重构**：主窗口视觉基线统一，全应用采用 .regularMaterial 背景、.rounded 字体、12pt/10pt 圆角规范
- **组件化提取**：HeaderButton、MetricCard、SettingsDivider、JobNoticeIcon 等可复用组件
- **macOS 版本适配**：symbolEffect(.pulse/.rotate) 等 macOS 15+ API 添加 #available 守卫，兼容 macOS 14
- **动效细节打磨**：hover 缩放、spring 动画参数优化，克制且有质感
- **四个页面全部覆盖**：UpdatesView（可升级列表）、ApplicationsView（本机应用）、SourcesView（管理来源）、JobsView（任务日志）+ SettingsView（设置页）+ ContentView（主框架）

### 涉及子 issue

- [LUC-210] 主窗口布局与视觉基线
- [LUC-211] 可升级与本机应用列表信息架构
- [LUC-212] 管理来源与设置页面板体验
- [LUC-213] 任务日志与动效细节

## v0.9.1 (2026-05-21)

### 新功能

- **开机自启动时自动隐藏 Dock 图标**：应用通过开机自启动方式启动时，自动读取设置中的 Dock 图标显示偏好。若用户已设置不显示 Dock 图标，则在自启动完成后自动隐藏，无需用户手动触发。（[LUC-190]）

### 基础设施

- **GitHub Actions Release 工作流**：添加 CI/CD 自动发布工作流

### 涉及子 issue

- [LUC-190] 实现 AppDelegate 提前设置 Dock 策略 + 启动时隐藏窗口

## v0.9.0 (2026-05-20)

### 性能优化

- **ContentView 拆分**：将 1618 行巨型视图拆分为独立视图文件（UpdatesView/AppsView/JobsView/LogView/SettingsView），改善 SwiftUI 渲染性能
- **扫描进度指示**：扫描期间显示当前阶段（本机应用/Homebrew/App Store），改善等待体验
- **搜索防抖**：200ms 防抖避免每次按键全量过滤，cachedUpgradeablePackages 和 availableUpdates 从 computed property 改为 @Published 缓存
- **Brew 扫描超时隔离**：各 brew 子命令独立超时（30s），单个失败不阻塞其他结果
- **JobsView 日志优化**：LazyVStack 逐行渲染，1500 条日志不卡顿
- **classify 算法优化**：O(apps × casks) → O(apps + casks)，normalizeToken 正则结果缓存

### 涉及子 issue
- [LUC-146] 扫描进度指示
- [LUC-147] ContentView 拆分
- [LUC-148] 搜索防抖 + 缓存 computed property
- [LUC-149] Brew 扫描超时独立处理
- [LUC-150] JobsView 日志 LazyVStack 优化
- [LUC-151] classify 算法优化 + normalizeToken 缓存

## [0.12.1] - 2026-05-29

### Fixed
- 添加完整 codesign 签名步骤到 build-native.sh，修复 macOS 应用「租车总成本比较」打开报错"可能已损坏或不完整"的问题 (LUC-251)
  - Sparkle.framework ad-hoc 签名
  - codesign --deep --force --sign - 整包签名
  - codesign --verify --deep --strict 验证
  - xattr -cr 清除隔离属性
